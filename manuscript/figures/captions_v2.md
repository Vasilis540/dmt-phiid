# Figure captions (draft_v2)

Generated by `scripts/15_figures_v2.py` at git 6f34e89-dirty, seed 20261120. Every value is read from the saved results files named under each figure.

## Figure 1

The sixteen Gaussian-MMI ΦID atoms of the whole-brain pair mean (6,555 pairs, 14 subjects, ts_gsr, windowed estimator W = 60), observed (blue) and AR(1)-substituted (green): the atoms of each pair's 4 × 4 matrix with its measured lag-1 autocorrelations a_x, a_y and lag-0 correlation q kept and its two cross-lag correlations replaced by a_y q and a_x q, evaluated per pair and window and averaged over pairs. (a) DMT pre-injection level (windows 1–4). (b) Primary difference-in-differences, DMT minus placebo, post (windows 6–14) minus pre (windows 1–4); whiskers are inverted sign-flip 95 % intervals of the mean over subjects (B21). Panel (a) carries no uncertainty: its bars are group means of the level. Atom codes: first letter the past-side type, last letter the future-side type (r redundant, x unique to region X, y unique to Y, s synergistic; e.g. rts = redundancy-to-synergy). The AR(1)-substituted atoms reproduce the structure of the table: the excess sts − (xtx + yty) is negative in the data (−0.0844) and in the substituted atoms (−0.1003); rts and str lie below xtx and yty; the four mirror atoms are negative. What the substitution leaves is the residual pattern of Table 1: sts −0.0530, the self-prediction atoms −0.0345 and −0.0345, the mirror atoms +0.0395 to +0.0400, the six cross-prediction atoms ±0.0231 where the substituted atoms are ±0.0027; the residual DiDs of the other fifteen atoms are at most 0.00143 in absolute value, against +0.0115 for sts. Source: `notes/review_results/partB/family_atoms_ts_gsr_W60.npz` (partB14).

## Figure 2

The scope map and the coupled family. (a) Gaussian-MMI sts of a bivariate AR(1) pair as a function of the pair's lag-1 autocorrelation r₁ and lag-0 cross-correlation q (closed form of Methods; grid step 0.01), with the density of one subject's saved pre-injection pairs — the overlay holds subject 1 only (ts_gsr; windows 1–4 of both runs, 52,440 pair × window points; contours at the 50th, 80th and 95th percentiles of the occupied cells; r₁ = mean of the two regions' within-window lag-1 autocorrelations, q = the pair's within-window lag-0 correlation; median r₁ 0.855, median |q| 0.249) and the operating point (0.85, 0.25) marked. (b) sts − (xtx + yty), which on the symmetric family equals rtr and is zero at q = 0. (c) sts against the coupling c of the symmetric VAR(1) pair x_{t+1} = a x_t + c y_t + ε, y_{t+1} = a y_t + c x_t + η, with a and the innovation correlation re-solved at each c so that the pair's r₁ and q stay fixed, at three points (solid); at the operating point sts is 1.2588 at c = 0, 1.2315 at +0.02, 1.2155 at +0.05 and 1.2569 at +0.10, and 1.3023 and 1.4111 at −0.02 and −0.05. The dashed curve is the same point with q = −0.25, obtained from the family's invariance under (c, q) → (−c, −q): coupling of the same sign as q lowers sts; of the opposite sign, raises it, and the response is not monotone. Source: `notes/rev_phiid_fast.py` (closed form), `notes/review_results/partB/scope_map_overlay_points.npz`, `notes/review_results/partB/coupling_map_tables.md` (matched view; partB8).

## Figure 3

Per-subject difference-in-differences (post windows 6–14 minus pre windows 1–4, DMT minus placebo; ts_gsr, W = 60; one marker per subject, labelled by index in (a) and (c)). The x-axis of (a) and (c) is the paper's r₁ contrast, the DiD of the regions' mean sample lag-1 autocorrelation; the generators' rates are per unit of their pair-level a, the mean of a pair's a_x and a_y, a second estimator of the same autocorrelation, each rate taken within one simulation and drawn through the origin. (a) Whole-brain MMI-sts DiD against the r₁ DiD: the OLS fit, +4.26 per unit (t interval, 12 df, [+3.41, +5.12]; intercept −0.018), and the band-passed generator's rate, 6.13 per unit of pair-level a (−0.09441 / −0.01540, B24); r = +0.953, Fisher-z 95 % interval [+0.854, +0.985]. Both DiDs are read off the same windows, so r includes estimation error they share. (b) The cross-half relation: each subject's sts DiD on the odd windows against its r₁ DiD on the even windows (filled circles, r = +0.742) and the sts DiD on the even windows against the r₁ DiD on the odd windows (open triangles, r = +0.645); mean r = +0.694, Fisher-z interval of the mean [+0.258, +0.895] (an approximation), against a ceiling of 0.729 set by the two halves' split-half reliabilities; disattenuated, +0.951 [+0.617, +0.991] (subject bootstrap, B21 (d)). (c) The residual DiD (observed minus AR(1)-substituted sts) against the r₁ DiD: the OLS fit, −0.75 per unit (t interval [−1.13, −0.37]; intercept +0.0005), and the rates of the band-passed generator (−0.18, B24), of AR(1) pairs (−0.39, B23 (b) (i)) and of the finite-sample null (−0.37: +0.0054 / −0.0146), per unit of pair-level a; on that basis the data's group means give −0.74 (+0.0115 / −0.0155, `residual_source.log`). Source: `notes/review_results/inference_rows_raw.pkl` and `inference_rows_diag.pkl` (field `did_subjects`, primary set); `notes/review_results/partB/splithalf_subjects.csv`, `inference_revision_tables.md` (d), `bandpassed_expectations_tables.md`, `diagnostic_alternatives_tables.md` (b), `residual_source.log`; `notes/review_results/logs/review_v2_residual_null.log`.

## Figure 4

The AR(1)-substituted estimate by window (ts_gsr, W = 60, N = 14; lines are group means). Shading is ± 1 within-subject SEM (Cousineau, 2005; Morey, 2008): each subject's values are centred on that subject's own mean before the SEM across subjects is taken, with Morey's correction, so the bands show the uncertainty of within-subject comparisons across windows and runs and not the between-subject spread of the level, which the paper does not test; the same band is drawn on the dashed lines. Grey band = pre-injection windows 1–4; hatched = window 5, excluded from the primary post set; dashed vertical line = injection at 8 min. (a) Observed whole-brain sts (solid) and the AR(1)-substituted sts (dashed): the sts of each pair's 4 × 4 matrix with its measured lag-1 autocorrelations a_x, a_y and lag-0 correlation q kept and its two cross-lag correlations replaced by a_y q and a_x q; DMT (red) and placebo (blue); y-range 1.00–1.30 nats. (b) The residual, observed minus AR(1)-substituted, per run, in the same units as (a) and with the same nats per unit height, so that the residual's modulation can be read against the size of the level and of the contrast. The AR(1)-substituted estimate exceeds the observed level by 0.049 nats on average, and its primary DiD is larger in size by 0.0115 nats (observed −0.0809, AR(1)-substituted −0.0924, residual DiD +0.0115). (c) The DMT − placebo difference of the residual per window (mean over subjects ± 1 within-subject SEM), with its expectation under a pure autocorrelation change as the dashed step: the pre-injection mean of the difference, then that mean plus the residual DiD that the calibration gives for an autocorrelation change of the data's size (+0.0027 ± 0.0014, condition (i) at W = 60, partB17b's band-passed generator; the grey band is ± 1 SD over replicate datasets). The dashed step, not zero, is the reference for reading the difference, because a pure autocorrelation change itself produces a positive residual DiD; Results 4 gives the data's exact sign-flip p against this and the two other calibrated expectations, +0.0049 and +0.0054 (0.108, 0.219 and 0.251; B21 (b)). Source: `notes/review_results/partB/diag_series_ts_gsr_W60.npz`; the expectation from `notes/review_results/partB/calibration_filtered_tables.md` when present, otherwise `calibration_tables.md`; the p values from `inference_revision_tables.md` (b).

## Figure 5

Lag dependence (S14 Table; ts_gsr, W = 60, N = 14; points are group-mean primary DiDs, DMT minus placebo, post windows 6–14 minus pre 1–4; whiskers are inverted sign-flip 95 % intervals of the mean over subjects (B21); the exact p of every point is in S14 Table). (a) The whole-brain MMI-sts DiD at τ = 1 (−0.0809), 2 (−0.0429), 3 (−0.0043), 5 (−0.0024) nats, with r, the per-subject correlation between the sts DiD and the lag-τ autocorrelation DiD (+0.953, +0.970, +0.626, −0.832). (b) The mean regional lag-τ autocorrelation DiD at the same lags (−0.0146, −0.0468, −0.0681, −0.0108); the x-axis labels give the DMT pre-injection mean r_τ (windows 1–4). Where the lag-τ autocorrelation contrast is negative (τ = 1, 2, 3) the sts contrast is negative, and at τ = 5, where the autocorrelation contrast is near zero, so is the sts contrast; the r_τ DiD grows in size with τ up to τ = 3 while the sts DiD shrinks with the atom, and at τ = 3 the per-subject tracking is only partial (Results 5). A longer lag reduces everything, the artefact included. Source: `notes/review_results/inference_rows_lag.pkl` (rows `sts tauN ts_gsr W60` and `autocorr lagN ts_gsr W60`, primary set).

## Figure 6

The regional map and regional autocorrelation (placebo run, pre-injection, ts_gsr, 115 regions, group means over 14 subjects). (a) Regional MMI-sts — the mean over a region's 114 pairs of the global-fit local sts, bins 1–8 — against the region's lag-1 autocorrelation r₁ (W = 60, windows 1–4), coloured by Yeo-7 network with the 16 subcortical parcels as one class (parcel counts in the legend); Pearson r = +0.863, least-squares slope +3.09 nats per unit r₁. (b) The pre-defined contrast between sensory cortex (visual and somatomotor networks, 31 parcels) and association cortex (default-mode and frontoparietal control networks, 37 parcels) of the sts map and of the sts − rtr map, before partialling (grey) and after regressing regional r₁ out of the group map (green): sts −0.0202 → +0.0037 nats, sts − rtr −0.0215 → −0.0014; the per-subject version and its sign-flip p are in Results 3. (c) The network means of the map with regional r₁ partialled out (± SEM over the network's parcels; parcel counts under the labels), from −0.0235 (Subcortex) to +0.0207 (DorsAttn), and the spin test of their structure (B22 (e); 10,000 rotations of the cortical parcels, each keeping its own class, the 16 subcortical values fixed): the eight-class one-way F is 8.146, spin p 0.0009, against 16.933, spin p 0.0564, for the unpartialled map; the residual map's somatomotor − default and visual − default contrasts are −0.00965 (spin p 0.2240) and +0.00875 (0.3089). Partialling removes the pre-defined sensory–association contrast but not all network structure. Source: `notes/review_results/partB/regional_partial.csv` (partB20); `notes/review_results/partB/aligned_directed_tables.md` (B22 (e)).

