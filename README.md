# Whole-brain synergistic information decreases under DMT

A secondary analysis of open DMT fMRI data (14 subjects, DMT and placebo runs,
Schaefer-116 parcellation, continuous subjective-intensity ratings) using
Integrated Information Decomposition (ΦID; Gaussian model, MMI redundancy,
lag 1) to test a pre-specified hypothesis: that whole-brain synergistic
information is up-regulated under DMT and tracks the subjective intensity of
the experience over the ~20-minute time course. Synergy is estimated per
region pair with the model refit in non-overlapping 60-TR windows, averaged
over all 6,555 pairs, and contrasted post- versus pre-injection on the DMT
run against the same contrast on the placebo run, with exact sign-flip and
phase-randomised nulls, motion control, and a second preprocessing variant.
The analysis plan, every decision rule and every prediction were written
into the repository and committed before the results they govern existed.

**Headline result.** Whole-brain synergy *decreases* after DMT relative to
placebo (difference-in-differences −0.081 nats [95 % CI −0.126, −0.038],
sign-flip p = 0.004, negative in 13 of 14 subjects; replicated without
global signal regression at −0.103; survives motion control; robust to
dropping any single subject), which refutes the pre-specified
up-regulation hypothesis and is reported as a refutation. Synergy correlates
with intensity across the decay phase, but that tracking claim is void under
the pre-specified time-in-scanner and motion controls, so the primary claim
is the step contrast plus the estimator characterisation; the decrease is
spatially uniform (114 of 115 regions), not concentrated in a proxy of the
synergistic workspace of Luppi et al. (2024), and no receptor map explains
its spatial pattern after correction.

## Repository layout

| path | contents |
|---|---|
| `manuscript/draft.md` | the paper (draft) |
| `manuscript/supplementary.md` | supplementary tables, every value quoted from `results/` |
| `manuscript/figures/` | Figures 1–3 (pdf, png) and `captions.md`, generated by `scripts/12_figures.py` |
| `manuscript/analysis_record.md` | the full pre-specification and results record, append-only, never retroactively edited |
| `manuscript/prespecification_summary.md` | **the audit trail**: each decision with the commit that fixed it, and whether it changed later |
| `scripts/00–13_*.py` | analysis scripts, numbered in execution order, each independently runnable |
| `results/` | every table and array, with the script name and git SHA in its header; `run_*.log` are the run logs |
| `run_all.sh` | regenerates everything from the raw `.mat` files (about 6 h single-core) |
| `requirements.lock.txt` | pinned environment (Python 3.12) |
| `CLAUDE.md` | orientation for further work: current state, standing methodological rules |
| `external/DMT_NCT/` | the source data repository, cloned locally and git-ignored (see below) |

## Reproducing

```bash
git clone <this repository> dmt-phiid && cd dmt-phiid
git clone https://github.com/singlesp/DMT_NCT.git external/DMT_NCT   # data; checked at commit 77af7aa
python3.12 -m venv .venv && .venv/bin/pip install -r requirements.lock.txt
# phyid is not on PyPI; the lock file pins it to a commit of
# https://github.com/Imperial-MIND-lab/integrated-info-decomp
.venv/bin/python scripts/00_verify.py     # data and method integrity check, seconds
bash run_all.sh                           # everything, ~6 h single-core; see the header for per-step times
```

Individual scripts can be run on their own once their inputs exist, for
example `scripts/06_primary_b_analysis.py --variant ts_gsr --window-trs 60`
for the primary inference (it reads the windowed atoms written by
`scripts/01_synergy_timecourse.py --fit-mode window`). All scripts use seed
20261120, write the git SHA of the working tree into their output headers,
and tag it `-dirty` if `scripts/` or the record had uncommitted changes.
Figures are regenerated from saved results only, never from a recomputation.

## Data source, citation and licence

The timeseries (four preprocessing variants), intensity ratings, framewise
displacement, EEG Lempel-Ziv regressor, structural connectome and serotonin
receptor maps are the files released in-repository by

> Singleton, S. P., et al. (2025). *Communications Biology*,
> doi:10.1038/s42003-025-08078-9. Code and data:
> https://github.com/singlesp/DMT_NCT, Zenodo doi:10.5281/zenodo.15177511.

acquired and first described by

> Timmermann, C., et al. (2023). *PNAS*, doi:10.1073/pnas.2218949120.

**Licence.** The source repository carries no licence file, and no
data-use agreement or request process is stated. Reuse terms are being
confirmed with the authors before the preprint is made public; until then
this repository does not redistribute the data (they are cloned into a
git-ignored directory) and the results here should be treated as derived
from data whose reuse terms are unconfirmed. This repository's own code and
text have no licence file yet either.

One data-quality note not documented in the source papers: subject index 7
(DMT run) has region 20 constant at zero in every variant, so region 20 is
dropped for all subjects and both conditions (115 regions, 6,555 pairs).

## Pre-specification and audit trail

Start with **`manuscript/prespecification_summary.md`**. It lists every
analysis decision, the commit and date that fixed it, and whether the
decision's text changed afterwards, including the predictions that failed.
The full record it summarises is `manuscript/analysis_record.md`, which
lived at `CLAUDE.md` until 14 Sep 2026 and was moved with `git mv`
(byte-identical; `git log --follow` carries its history). The record is a
git-tracked file, not an external pre-registration: what it establishes is
ordering within the commit history, nothing more.

Contact: Vasilis Sampalis, sampalisvasilis@gmail.com.
